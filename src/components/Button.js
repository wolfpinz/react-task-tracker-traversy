// import PropTypes from 'prop-types'

const Button =  ({ onAdd })  => {
  return (
    <div
      className='btn'
      onClick={onAdd}
      >
      Add
    </div>
  )
}

// Button.propTypes = {

// }

export default Button
