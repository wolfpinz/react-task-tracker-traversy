// import PropTypes from 'prop-types'
import Button from './Button'

const Header = props => {
  return (
    <div className='header'>
      <h1>Task Tracker</h1>
      <Button />
    </div>
  )
}

// Header.propTypes = {
//
// }

export default Header
